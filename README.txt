How do you think XML relates to HTML?
    Just as it was described in class HTML is just a small part of XML. XML allows more freedom with defining different attributes, and details to better fit what I am doing.

What are some benefits of something like HTML using XML's syntax?
    A computer is better able to sift through data from the XML syntax than an HTML document.
What are some disadvantages?
    XML is not as human friendly as far as readability.